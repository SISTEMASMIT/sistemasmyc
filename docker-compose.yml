# Servidor nginx
services:
  web:
    image: nginx:1.23    # Puerto 8080 
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./config/docker.conf:/etc/nginx/conf.d/default.conf
    links:
      - php-fpm

  # Configuración de php-fpm
  php-fpm:
    build: ./phpDockerFile/
    volumes:
      - .:/var/www/html
